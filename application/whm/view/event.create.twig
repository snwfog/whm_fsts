{% extends 'static/base.twig' %}

{% block content %}
    <div class="span6">
        {% for error in errors %}
            {% block error %}
                <div id="error">{{ error }}</div></br>
            {% endblock %}
        {% endfor %}

        <form action="{{ apppath }}event/new" method="POST" name="event-form" id="view-event-form">

            <legend class ='controls controls-row'>
                <div class='pull-left'>
                    ID: {{ event["event-id"] }}
                </div>
                 <div class='pull-right'>
                    <span class="label label-success"><i class="icon-user icon-white"></i> 23/100</span>
                </div>
            </legend>

            {% include 'base.createEvent.part.twig' %}

            {% include 'date.createEvent.part.twig' %}

            {% include 'info.createEvent.part.twig' %}

        
            <legend>Action</legend>
            <div class="btn-toolbar">
                <button id="event-create-modify" type="button" class="btn btn-large btn-warning" aria-hidden="true"><i class="icon-edit icon-white"></i> Modify</button>
                <button id="create-event-save" type="button" class="btn btn-large btn-success"><i class="icon-plus icon-white"></i><i class="icon-tasks icon-white"></i> Create Event</button>
                <!--<button id="load-event-template" class="btn btn-info"><i class="icon-file icon-white"></i> Load Template </button>-->
                <button id="create-template-save" type="button" class="btn btn-large btn-info"><i class="icon-file icon-white submit"></i> Save Template </button>
            </div>
        
            <input type="hidden" name="event-id" value="{{ event["event_id"] }}"/>
            <input type="hidden" name="template-id" value="{{ template["template-id"] }}"/>

        </form>

        {% include 'template.list.twig' %}
    </div>
{% endblock content %}

{# This part should be auto generated #}
{% block left1 %}
    <div class="span3">
        <legend>Templates</legend>
        <div class="well" id="events-well">
            <ul class="nav nav-list" id="event-list">
                {% for template in event.templates %}
                    {% if(template.principal and template.active) %}
                        <li class="active"><a href="{{ apppath }}/event/{{ template.template_id}}/{{ template["template-id"]}}">
                                <i class="icon-home"></i> {{member["name"] }}</a></li>

                    {% elseif template.active %}
                        <li class="active"><a href="{{ apppath }}/event/{{ template.template_id}}/{{ template["template-id"]}}">
                                <i class="icon-home"></i> {{member["name"] }}</a></li>

                    {% elseif template.principal %}
                        <li class="active"><a href="{{ apppath }}/event/{{ template.template_id}}/{{ template["template-id"]}}">
                                <i class="icon-home"></i> {{member["name"] }}</a></li>

                    {% else%}
                        <li class="active"><a href="{{ apppath }}/event/{{ template.template_id}}/{{ template["template-id"]}}">
                                <i class="icon-home"></i> {{member["name"] }}</a></li>
                    {% endif %}
                {% endfor %}
            </ul>
        </div>
            
            <button class="btn btn-large btn-success" 
                        id="load-template-submit-btn"
                        name="load-template-submit-btn" type="button"
                        data-toggle="modal" data-target="#load-template-modal">
                    <i class="icon-file icon-white submit"></i>Load Template
            </button>
           
    </div>
{% endblock left1 %}

{% block left2 %}
       <div class="span3">
        <legend>Events</legend>
        <div class="well" id="events-well">            <thead>
        <table class ='table table-hover controls controls-row'>
            </thead>
            <tbody>
                <tr>
                    <td>Christmas Food Giveaways</td>
                </tr>
                <tr>
                    <td>Back to School Supplies</td>
                </tr>
                <tr>
                    <td>Matresse Giveaways</td>
                </tr>

            </tbody>



        </table>
          <!--  <ul class="nav nav-list" id="event-list">
                {% for member in household.members %}
                    {% if(member.principal and member.active) %}
                        <li class="active"><a href="{{ apppath }}/household/{{ household.household_id}}/{{ member["member-id"]}}">
                                <i class="icon-home"></i> {{member["first-name"] }} {{ member["last-name"]}}</a></li>

                    {% elseif member.active %}
                        <li class="active"><a href="{{ apppath }}/household/{{ household.household_id}}/{{ member["member-id"]}}">
                                <i class="icon-user"></i> {{member["first-name"] }} {{ member["last-name"]}}</a></li>

                    {% elseif member.principal %}
                        <li><a href="{{ apppath }}/household/{{ household.household_id}}/{{ member["member-id"]}}">
                                <i class="icon-home"></i> {{member["first-name"] }} {{ member["last-name"]}}</a></li>

                    {% else%}
                        <li><a href="{{ apppath }}/household/{{ household.household_id}}/{{ member["member-id"]}}">
                                <i class="icon-user"></i> {{member["first-name"] }} {{ member["last-name"]}}</a></li>
                    {% endif %}
                {% endfor %}
            </ul>-->
        </div> 
                     
    </div>
{% endblock left2 %}
