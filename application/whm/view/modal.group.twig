{% block modal_group %}
    <div class="container-fluid">
        <div class="row-fluid">

            {# NAVBAR MODAL GROUP #}
            {# COMPONENTS HERE WILL PERSIST IN THE ENTIRE #}
            {# USER SESSION. PLACE THEM HERE WITH CARE. #}
            {% include 'addhousehold.modal.twig' %}
            {% include 'createEvent.modal.twig' %}
            {% include 'search.modal.twig' %}

            {# Weird place to put this... but I cannot see where else to put it atm. - Charles #}
            {% include 'manageflag.modal.twig' %}

        </div>
    </div>
{% endblock modal_group %}